<div class="flex items-center justify-center h-screen w-screen" data-controller="login">
  <div class="flex flex-col gap-5 border-solid border-gray-500 border rounded p-5">
    <div class="flex flex-col gap-2" >
      <%= render partial: "login_btn" %>
      <%= render partial: "login_msg" %>

      <turbo-frame id="error_msg">
        <div
          class="hidden bg-red-100 border-t border-b border-red-500 text-red-700 px-4 py-3 text-sm"
          data-login-target="errorMsg">
        </div>
      </turbo-frame>
    </div>
  </div>